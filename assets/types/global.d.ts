interface Window {
	config: {
		DEFAULT_SHIFT_CAPACITY: number;
		DEFAULT_SCHEDULE: Record<string, number[]>;
		MAXIMUM_SUBSCRIPTIONS: number;
		SUBSCRIPTION_TIME_LIMIT: number;
		UNSUBSCRIPTION_TIME_LIMIT: number;
	};
}
